# Pod bootstrap: installed in STAGES by scripts/runpod_bootstrap.sh (not `pip install -r` in one shot).
# Do NOT list torch here — use the RunPod image's PyTorch (avoids pip "resolution-too-deep").
#
# Stage 1 (core-ml.txt): transformers, peft, bnb, datasets, etc.
# Stage 2 (extras.txt): liger, pandas, evalplus
# Stage 3: axolotl via --no-build-isolation (see bootstrap script)
